# VitePress 使用指南

## 项目已成功重构为 VitePress！

### ✅ 已完成的工作

1. **安装 VitePress** - 已配置好开发环境
2. **配置导航和侧边栏** - 自动生成目录结构
3. **创建首页** - 美观的 Hero 布局和特性展示
4. **自定义主题** - 优化了表格、代码块、链接等样式
5. **部署配置** - 支持 GitHub Pages 自动部署

### 🚀 快速开始

```bash
# 1. 进入项目目录
cd interview-guide

# 2. 安装依赖（如果还没安装）
npm install

# 3. 启动开发服务器
npm run docs:dev

# 4. 访问 http://localhost:5173
```

### 📁 项目结构

```
interview-guide/
├── .vitepress/
│   ├── config.mts          # VitePress 配置
│   ├── theme/
│   │   ├── index.ts        # 主题入口
│   │   └── custom.css      # 自定义样式
│   └── dist/               # 构建输出（自动生成）
├── questions/              # 题目文档（保持不变）
├── code-examples/          # 代码示例（保持不变）
├── knowledge-map/          # 知识图谱（保持不变）
├── daily-logs/             # 学习日志（保持不变）
├── public/                 # 静态资源
│   ├── logo.svg
│   └── hero-image.svg
├── index.md                # 首页
├── package.json
└── README.md
```

### 🎨 主要特性

#### 1. 美观的首页
- Hero 区域展示项目特点
- 统计数据卡片
- 分类导航卡片
- 响应式设计

#### 2. 智能导航
- 顶部导航：CSS、JavaScript、Vue、知识图谱
- 侧边栏：自动根据当前路径显示相关题目
- 面包屑导航

#### 3. 搜索功能
- 内置本地搜索
- 支持全文搜索
- 快捷键：`Ctrl/Cmd + K`

#### 4. 代码高亮
- 支持多种语言
- 行号显示
- 代码复制按钮
- 明暗主题切换

#### 5. 移动端优化
- 响应式布局
- 移动端菜单
- 触摸友好

### 📝 添加新内容

#### 添加新题目

1. 在对应目录创建 markdown 文件
2. 更新 `.vitepress/config.mts` 中的侧边栏配置
3. 更新 `knowledge-map/index.md` 索引

#### 修改配置

编辑 `.vitepress/config.mts`：

```typescript
export default defineConfig({
  title: "你的标题",
  description: "你的描述",
  themeConfig: {
    nav: [...],      // 顶部导航
    sidebar: {...},  // 侧边栏
    // 更多配置...
  }
})
```

#### 自定义样式

编辑 `.vitepress/theme/custom.css` 添加自定义样式。

### 🚀 部署到线上

#### 方式一：GitHub Pages（推荐）

1. 将代码推送到 GitHub
2. 在仓库设置中启用 GitHub Pages
3. 选择 "GitHub Actions" 作为部署源
4. 推送到 main 分支自动部署

#### 方式二：Vercel

1. 导入 GitHub 仓库到 Vercel
2. Vercel 自动识别 VitePress 项目
3. 自动部署，获得域名

#### 方式三：Netlify

1. 连接 GitHub 仓库
2. 设置构建命令：`npm run docs:build`
3. 设置发布目录：`.vitepress/dist`
4. 部署

#### 方式四：手动部署

```bash
# 构建
npm run docs:build

# 将 .vitepress/dist 目录部署到任何静态托管服务
```

### 🎯 优化建议

#### 1. SEO 优化

在每个 markdown 文件顶部添加 frontmatter：

```yaml
---
title: 页面标题
description: 页面描述
head:
  - - meta
    - name: keywords
      content: 关键词1, 关键词2
---
```

#### 2. 性能优化

- 图片使用 WebP 格式
- 启用 gzip 压缩
- 使用 CDN 加速

#### 3. 添加评论系统

可以集成 Giscus、Gitalk 等评论系统。

#### 4. 添加统计分析

可以集成 Google Analytics、百度统计等。

### 📚 VitePress 文档

- [官方文档](https://vitepress.dev/)
- [配置参考](https://vitepress.dev/reference/site-config)
- [主题配置](https://vitepress.dev/reference/default-theme-config)
- [Markdown 扩展](https://vitepress.dev/guide/markdown)

### 🐛 常见问题

#### Q: 如何修改主题颜色？

A: 在 `custom.css` 中覆盖 CSS 变量：

```css
:root {
  --vp-c-brand-1: #your-color;
  --vp-c-brand-2: #your-color;
}
```

#### Q: 如何添加自定义组件？

A: 在 `.vitepress/theme/index.ts` 中注册 Vue 组件。

#### Q: 构建失败怎么办？

A: 检查：
1. Node.js 版本（建议 18+）
2. 依赖是否完整安装
3. markdown 文件语法是否正确

### 🎉 下一步

1. **本地预览**：运行 `npm run docs:dev` 查看效果
2. **自定义内容**：修改首页、添加新题目
3. **部署上线**：选择一个部署平台发布
4. **分享链接**：将网站分享给其他人

---

**提示**：VitePress 开发服务器已启动，访问 http://localhost:5173 查看效果！
